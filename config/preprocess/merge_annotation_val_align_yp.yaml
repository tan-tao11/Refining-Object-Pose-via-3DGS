type: merge_anno
task_name: merge_anno_align
# train_model_name: yp_model_5_1_cropped #supply on the command line
# test_model_name: yp_model_5_1_cropped #supply on the command line
# out_model_name: yp_model_5_1_cropped #supply on the command line

split: 'val'

data_base_dir: dataset/${test_model_name}
gs_dir: output/gs_models/${train_model_name}
merge_output_dir: output/merged_anno/${out_model_name}

val_use_last_n_seq: 1
names:
  - 'model-ship'


datamodule:
    data_dir: ${data_base_dir}
    gs_dir: ${gs_dir}

    out_path: ${merge_output_dir}/${task_name}/${split}.json
